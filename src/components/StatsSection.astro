---
import type { StatItem } from "../types";

const stats: StatItem[] = [
    {
        value: "Logi",
        label: "Jedyne źródło prawdy",
        description:
            "Jedyne miejsce, w którym zobaczysz, co Googlebot naprawdę odwiedził. Reszta to domysły.",
    },
    {
        value: "Budżet",
        label: "Crawl budget przy skali",
        description:
            "Przy dużej strukturze nie liczy się, ile masz stron. Liczy się, ile Google zdąży odwiedzić.",
    },
    {
        value: "Migracja",
        label: "Jedna szansa",
        description:
            "Zdarza się raz i nie da się jej cofnąć. Albo jest zaplanowana, albo kosztuje.",
    },
];
---

<section class="py-32 px-6 md:px-12 bg-[#0a0a0a] text-[#f4f4f4]">
    <div class="max-w-7xl mx-auto">
        <div
            class="flex flex-col md:flex-row justify-between items-start md:items-end mb-20 border-b border-white/20 pb-8"
        >
            <h2
                class="font-display text-4xl md:text-6xl font-bold uppercase tracking-tighter"
            >
                Dlaczego techniczne SEO?
            </h2>
            <p
                class="font-mono text-xs mt-4 md:mt-0 uppercase tracking-widest opacity-60 text-right"
            >
                Dane nie kłamią
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-8">
            {
                stats.map((stat) => (
                    <div class="group interactive">
                        <div class="text-5xl md:text-7xl font-display font-bold mb-4 bg-clip-text text-transparent bg-linear-to-r from-white to-gray-500">
                            {stat.value}
                        </div>
                        <div class="h-px w-12 bg-white/30 mb-6 group-hover:w-full transition-all duration-500" />
                        <h3 class="font-bold text-lg mb-2">{stat.label}</h3>
                        <p class="text-white/60 text-sm leading-relaxed">
                            {stat.description}
                        </p>
                    </div>
                ))
            }
        </div>
    </div>
</section>
